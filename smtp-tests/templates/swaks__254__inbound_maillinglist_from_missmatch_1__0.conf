# Connection
--server ${MAILSERVER_NAME}:25
--tls
-4
# Greeting
--helo ${HELO_NAME}
# Envelope
--to ${SENDER_ADDRESS_MAIN}
--from ${SENDER_MAILLINGLIST}
# DATA
--h-Subject Swaks SMTP test - Sender mismatch / maillinglist inbound
--h-From: ${SENDER_MAILLINGLIST_ORIGIN}
--body @${TEMPLATE_PATH}/swaks__254__inbound_maillinglist_from_missmatch_1.txt
# END
