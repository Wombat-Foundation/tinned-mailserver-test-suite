# EXPECTED_RC=0
# CATEGORY="outbound,smtps,authenticated"
#
# Connection
--server ${MAILSERVER_NAME}:465
--tls-on-connect
-4
# Greeting
--helo ${HELO_NAME}
# Authentication
--auth PLAIN
--auth-user ${SENDER_AUTH_USER}
--auth-password ${SENDER_AUTH_PASSWORD}
# Envelope
--to ${RECIPIENT_ADDRESS}
--from ${SENDER_ADDRESS_ALIAS_TAG}
# DATA
--h-Subject Swaks SMTP test - Sender plain authenticated alias+extension address
--body @${TEMPLATE_PATH}/swaks__114__outbound_smtps_plain-authenticated_from_alias+ext.txt
# END
