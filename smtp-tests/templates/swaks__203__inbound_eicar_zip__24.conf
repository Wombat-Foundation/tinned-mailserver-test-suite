# Connection
--server ${MAILSERVER_NAME}:25
--tls
-4
# Greeting
--hello localhost
# Envelope
--to ${SENDER_ADDRESS_MAIN}
--from ${RECIPIENT_ADDRESS}
# DATA
--h-Subject Swaks SMTP test - Inbound EICAR.COM-ZIP
--body @${TEMPLATE_PATH}/swaks__203__inbound_eicar_zip.txt
# Attachment
--attach-name EICAR.COM-ZIP.zip
--attach @${PAYLOAD_PATH}/EICAR.COM-ZIP
# END
