# EXPECTED_RC=0
# CATEGORY="outbound,submission,authenticated"
#
# Connection
--server ${MAILSERVER_NAME}:587
--tls
-4
# Greeting
--helo ${HELO_NAME}
# Authentication
--auth PLAIN
--auth-user ${SENDER_AUTH_USER}
--auth-password ${SENDER_AUTH_PASSWORD}
# Envelope
--to ${RECIPIENT_ADDRESS}
--from ${SENDER_ADDRESS_MAIN}
# DATA
--h-Subject Swaks SMTP test - Sender plain authenticated main address
--body @${TEMPLATE_PATH}/swaks__101__outbound_submissions_plain-authenticated_from_main.txt
# END
