# EXPECTED_RC=0
# CATEGORY="outbound,smtps,authenticated"
#
# Connection
--server ${MAILSERVER_NAME}:465
--tls-on-connect
-4
# Greeting
--helo ${HELO_NAME}
# Authentication
--auth LOGIN
--auth-user ${SENDER_AUTH_USER}
--auth-password ${SENDER_AUTH_PASSWORD}
# Envelope
--to ${RECIPIENT_ADDRESS}
--from ${SENDER_ADDRESS_MAIN}
# DATA
--h-Subject Swaks SMTP test - Sender login authenticated main address
--body @${TEMPLATE_PATH}/swaks__115__outbound_smtps_login-authenticated_from_main.txt
# END
